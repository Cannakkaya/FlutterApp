import 'package:flutter/material.dart';

class PsikologScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Psikolog Dashboard')),
      body: Center(
        child: Text('Welcome Psikolog!'),
      ),
    );
  }
}
