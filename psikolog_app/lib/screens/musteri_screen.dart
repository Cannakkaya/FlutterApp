import 'package:flutter/material.dart';

class MusteriScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Müşteri Dashboard')),
      body: Center(
        child: Text('Welcome Müşteri!'),
      ),
    );
  }
}
