import 'package:flutter/material.dart';

class AsistanScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Asistan Dashboard')),
      body: Center(
        child: Text('Welcome Asistan!'),
      ),
    );
  }
}
